# ============================================
# WATSONX HACKATHON - SECURITY .GITIGNORE
# ============================================
# DO NOT REMOVE OR MODIFY SECURITY PATTERNS
# ============================================

# ============================================
# AI ASSISTANT SESSION FILES (CRITICAL)
# ============================================
# NOTE: bob_sessions/ folder is REQUIRED for project submission
# Live session files are excluded, but exported reports must be included
.copilot/
.cursor/
.codeium/

# ============================================
# CREDENTIALS AND SECRETS (CRITICAL)
# ============================================

# Environment files
.env
.env.*
!.env.example
*.env
env.local
.env.local
.env.*.local

# IBM Cloud credentials
ibmcloud-credentials.json
ibm-credentials.env
*-credentials.json
*apikey*
*api-key*
*api_key*

# Configuration files with credentials
config.json
config.yaml
config.yml
secrets.json
secrets.yaml
secrets.yml
credentials.json
credentials.yaml

# Private keys
*.pem
*.key
*.p12
*.pfx
*.cer
*.crt
id_rsa
id_dsa
*.ppk

# Generic credential patterns
*credentials*
*secret*
*password*
*token*

# Database credentials
database.yml
database.json
db-config.*

# ============================================
# DEVELOPMENT FILES
# ============================================

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv

# Java
*.class
*.jar
*.war
target/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Build outputs
dist/
build/
coverage/
*.log

# Backup files
*.bak
*.tmp
*.temp
*~
.backup/

# ============================================
# DO NOT REMOVE ABOVE PATTERNS
# Add project-specific patterns below
# ============================================
# ============================================
# HINDSIGHT — patterns do projeto
# ============================================

# Diretorio de segredos (explicito, alem do padrao *secret* acima)
secrets/
config/credentials.json

# Clone do repositorio-benchmark: nunca entra neste repo.
# O estado virgem dele e o baseline do experimento.
bob-demo/
bob-get-started/

# Exports brutos de sessao que ainda nao foram auditados.
# Mover para benchmark/ ou fixtures/ so depois de conferir que nao ha credencial.
*.raw-export.json

# Screenshots brutos podem exibir prompt, transcript ou caminho privado.
*.raw-summary.png

# Bob task export files
bob-task-*.json
bob-task-*.md
# Multi-task exports (Tasks -> export all). Raw, unredacted, and large.
bob-tasks-*.json
bob-tasks-*.md
.bob/tmp/
