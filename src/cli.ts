/**
 * CLI entry point — Hindsight (stub)
 *
 * Responsibility: orchestrate parser → observe → diagnose → prescribe → compare
 * and present results to the user. The only module allowed to call all others
 * in sequence.
 *
 * Redacts sensitive data (message content, tool arguments) before display.
 * Demo mode loads fixtures/sample-export.json — no credentials, no network.
 *
 * Implemented in F6 (#21–#24).
 */

console.log("Hindsight — use --help once the CLI is implemented (F6).");
